{% extends 'template.html.twig' %}

{% block title %}Person{% endblock %}

{% block bodyTitle %} Détails Person {% endblock %}

{% block body %}
    {{ dump(person) }}
    <div class="alert alert-success">
        <h4>Firstname = {{ person.firstname }}</h4>
        <h4>Name = {{ person.name }}</h4>
        <h4>Old = {{ person.old }}</h4>
    </div>
{% endblock %}
