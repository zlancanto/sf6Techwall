{#
    A un traitement métier
    Utilise un controller (FirstController)
#}
<h1>Hello {{ name| upper }} {{ firstname | capitalize }} !</h1>

{#
    ~ = concaténation en twig
    <img src="{{ asset('assets/images/') ~ image | defaultImage }}" alt="">
#}
